# TODO: Complétez ce workflow
#
# Objectif : Créer un pipeline CI qui :
# 1. Se déclenche sur push vers main ET sur pull_request
# 2. Récupère le code
# 3. Installe Python 3.11
# 4. Installe les dépendances
# 5. Lance les tests pytest

name: CI

# TODO: Configurer les déclencheurs
# Indice: on: push (branches), pull_request
on:
  # À compléter...

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      # TODO: Étape 1 - Checkout du code
      # Indice: uses: actions/checkout@...

      # TODO: Étape 2 - Installer Python
      # Indice: uses: actions/setup-python@...
      # N'oubliez pas with: python-version

      # TODO: Étape 3 - Installer les dépendances
      # Indice: run: pip install -r requirements.txt

      # TODO: Étape 4 - Lancer les tests
      # Indice: run: pytest

